///|
/// Managed TEA layer for the CodeMirror–Rabbita binding.
///
/// This package will expose `State`, `Action`, `Event`, `update`,
/// `subscriptions`, and `view`. It must not contain `extern "js"`
/// declarations — those live in the `js/` subpackage.
