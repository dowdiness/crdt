{
  "name": "canopy-block-editor-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "npm run prebuild:moonbit",
    "prebuild:moonbit": "cd .. && moon build --target js --release"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
