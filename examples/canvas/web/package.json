{
  "name": "canopy-canvas",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild:moonbit": "cd .. && moon build --target js --release"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
